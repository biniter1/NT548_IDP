apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: finaltestforgolden
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: backstage
    owner: devops-team
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    backstage.io/managed-by-location: "url:https://github.com/biniter1/finaltestforgolden"
spec:
  project: default
  source:
    repoURL: https://github.com/biniter1/finaltestforgolden.git
    targetRevision: main
    path: helm
    helm:
      values: |
        image:
          repository: ghcr.io/biniter1/finaltestforgolden
          tag: latest
        service:
          port: 8080
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        replicaCount: 1
  destination:
    server: https://kubernetes.default.svc
    namespace: finaltestforgolden
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
